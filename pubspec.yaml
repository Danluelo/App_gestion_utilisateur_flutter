name: my_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # HTTP pour consommer API REST
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5   # <--- ajoute ça
  http: ^1.2.2

  # Firebase
  firebase_core: ^4.1.0
  cloud_firestore: ^6.0.1

  # Icônes Cupertino (iOS style)
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
